{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the Fieldnote shell. Deliberately minimal: the interface itself runs from the local backend over HTTP and needs no native APIs.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:default",
    "opener:allow-open-url"
  ]
}
